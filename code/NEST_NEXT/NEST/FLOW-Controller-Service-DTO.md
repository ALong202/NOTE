# Flow Controller → Service → DTO


```js
─────────────────────────────────────────────────────────────
🚀 ỨNG DỤNG NESTJS KHỞI ĐỘNG
─────────────────────────────────────────────────────────────
bootstrap()        NestFactory.create(AppModule)
│
▼
+-------------------+
|     AppModule     |
|-------------------|
| controllers: [...]|
| providers: [...]  |
+---------+---------+
│
▼
─────────────────────────────────────────────────────────────
🔁 DEPENDENCY INJECTION CONTAINER
─────────────────────────────────────────────────────────────
NestJS quét toàn bộ class có decorator:
@Controller()  → đăng ký endpoint
@Injectable()  → đăng ký provider (service, repo, v.v.)

Ví dụ:
+-------------------------------------------+
| @Injectable()                             |
| export class UsersService {...}           |  ← được đăng ký là provider
+-------------------------------------------+

+-------------------------------------------+
| @Controller('users')                      |
| export class UsersController {...}        |  ← được đăng ký là controller
+-------------------------------------------+
─────────────────────────────────────────────────────────────
⬇ Khi có request HTTP
─────────────────────────────────────────────────────────────
🧍 Client gửi request POST /users
với body JSON:
{
"name": "Nam",
"email": "[nam@gmail.com](mailto:nam@gmail.com)"
}

─────────────────────────────────────────────────────────────
Controller nhận request
─────────────────────────────────────────────────────────────
+------------------------------------------------------------+
| @Controller('users')                                       |
| export class UsersController {                             |
|   constructor(private readonly usersService: UsersService){}|
|                                                            |
|   @Post()                                                  |
|   create(@Body() dto: CreateUserDto) {                     |
|       return this.usersService.createUser(dto);            |
|   }                                                        |
| }                                                          |
+------------------------------------------------------------+

Giải thích:

- Nest tự inject UsersService vào constructor.
- DTO (`CreateUserDto`) lấy dữ liệu runtime từ request body.
- Controller chỉ gọi service, không xử lý logic.

─────────────────────────────────────────────────────────────
Service xử lý logic (được inject vào Controller)
─────────────────────────────────────────────────────────────
+------------------------------------------------------------+
| @Injectable()                                              |
| export class UsersService {                                |
|   createUser(data: CreateUserDto) {                        |
|       // Xử lý dữ liệu, lưu DB, hoặc gọi API khác          |
|       return { success: true, data };                      |
|   }                                                        |
| }                                                          |
+------------------------------------------------------------+

─────────────────────────────────────────────────────────────
Sau khi service trả kết quả
─────────────────────────────────────────────────────────────
Controller → Response → Trả lại client JSON:
{
"success": true,
"data": { "name": "Nam", "email": "[nam@gmail.com](mailto:nam@gmail.com)" }
}
─────────────────────────────────────────────────────────────

```